type User {
  id: Int!
  email: String!
  role: String!
  expensiveField: Int!
}

type Query {
  users: [User!]!
}

type Mutation {
  createUser(user: UserInput!): Boolean
}

input UserInput {
  Email: String!
  PasswordHash: String!
}
